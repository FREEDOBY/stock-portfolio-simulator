# 프론트엔드 환경변수 예시
# 로컬 개발: 이 값을 비워두면 '/api'로 요청 → vite.config.ts 프록시가 localhost:8000으로 전달
# 프로덕션(Vercel): Render 백엔드 주소 + '/api' 를 지정한다.
#
# 주의: 백엔드 라우터가 '/api/...' prefix를 쓰므로 반드시 끝에 '/api'를 붙일 것.
# 예: VITE_API_URL=https://stock-portfolio-simulator-api.onrender.com/api
VITE_API_URL=
